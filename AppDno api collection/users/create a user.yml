info:
  name: create a user
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:5143/api/user
  body:
    type: json
    data: |-
      {
        "Email" : "admin@t.t",
        "FirstName" : "nom",
        "LastName" : "prenom",
        "Group" : "avignon",
        "Role" : 0
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
