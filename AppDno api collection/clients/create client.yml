info:
  name: create client
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:5143/api/client
  body:
    type: json
    data: |-
      {
        "Name" : "client best"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
