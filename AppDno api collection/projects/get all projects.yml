info:
  name: get all projects
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:5143/api/project/all
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
