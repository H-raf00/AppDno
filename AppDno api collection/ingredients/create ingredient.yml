info:
  name: create ingredient
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:5143/api/ingredient
  body:
    type: json
    data: |-
      {
        "Name" : "ing2",
        "SupplierId" : 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
