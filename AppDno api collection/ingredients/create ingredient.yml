info:
  name: create ingredient
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:5143/api/ingredient
  body:
    type: json
    data: |-
      {
        "Name" : "i4044526",
        "SupplierId" : 1,
        "Status" : 0
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
