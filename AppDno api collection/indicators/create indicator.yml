info:
  name: create indicator
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:5143/api/indicator
  body:
    type: json
    data: |-
      {
        "Name" : "desc",
        "Description" : "azeatgzgregretjhnryj",
        "Type" : "ph"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
