info:
  name: create indicator
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:5143/api/indicator
  body:
    type: json
    data: |-
      {
        "Name" : "ind56146",
        "Type" : "int"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
