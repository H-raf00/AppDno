info:
  name: create a user
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:5143/api/user
  body:
    type: json
    data: |-
      {
        "Email" : "t3@t.t",
        "FirstName" : "name3",
        "LastName" : "lname3",
        "Group" : "paris",
        "Role" : 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
