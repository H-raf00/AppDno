info:
  name: create a user using email
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:5143/api/user
  body:
    type: json
    data: |-
      {
        "Email" : "test2@t.t"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
