info:
  name: create supplier
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:5143/api/supplier
  body:
    type: json
    data: |-
      {
        "Name" : "supplier 1"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
